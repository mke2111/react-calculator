import React from 'react';

const Quote = () => (
  <div>
    <h1>Quote of the day</h1>
    <p>
      This simple calculator is made with react. The project ensures proper user of routing in react
    </p>
  </div>
);

export default Quote;
